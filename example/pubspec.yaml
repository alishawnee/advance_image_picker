name: example
description: Example for advance_image_picker

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <=4.0.0"
  flutter:


dependencies:
  advance_image_picker:
    path: ../

  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  intl:


dev_dependencies:
  flutter_test:
    sdk: flutter

# dependency_overrides:
#   platform: ^3.1.0

flutter:
  uses-material-design: true

  assets:
  - assets/icon/
